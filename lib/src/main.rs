fn main() {
    // let mut acc = make_acc();
    // println!("Acc sm: {:?}", acc.tranduce(0..10).collect::<Vec<_>>());
    // let mut acc = make_acc();
    // println!(
    //     "Acc sm: {:?}",
    //     acc.tranduce((0..10).map(|i| i as f64 * 1.0))
    //         .collect::<Vec<_>>()
    // );
    // let mut delay = make_delay(1);
    // println!("Delay: {:?}", delay.tranduce(10..20).collect::<Vec<_>>());
    // println!("Delay: {:?}", delay.tranduce(20..30).collect::<Vec<_>>());
    // println!("Delay: {:?}", delay.tranduce(30..40).collect::<Vec<_>>());
    // let mut gain = make_gain(2.0);
    // println!(
    //     "Gain sm: {:?}",
    //     gain.tranduce((0..10).map(|i| i as f64 * 1.0))
    //         .collect::<Vec<_>>()
    // );
    // println!(
    //     "delay Gain sm: {:?}",
    //     gain.cascade(make_delay(1.0))
    //         .tranduce((0..10).map(|i| i as f64 * 1.0))
    //         .collect::<Vec<_>>()
    // );
}
